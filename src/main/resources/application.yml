---

spring:
  profiles:
    active: dev
  data:
    mongodb:
      host: localhost
      port: 27017
      database: moviedb

app:
  name: movieservice
  description: ${app.name} is a Rest API to provide the movies list
welcome:
  message: welcome to ${app.name}

server:
  port: 8081
  servlet:
    context-path: /movies

---

spring:
  profiles: dev


---

spring:
  profiles: prod

